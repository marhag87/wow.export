{
	"name": "screencap",
	"version": "1.0.0",
	"description": "screen region capture n-api (windows only)",
	"main": "index.js",
	"scripts": {
		"install": "echo 'Skipping automatic native build - use build script instead'"
	},
	"dependencies": {
		"node-addon-api": "^8.5.0"
	}
}
